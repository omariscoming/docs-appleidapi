# شهر اپل آیدی
## خلاصه
| درباره ما | شهر اپل آیدی |
|:------------|:---------:|
|شهر اپل آیدی، مقصدی مطمئن برای خرید انواع اپل آیدی، چه به‌صورت تکی، چه همکاری عمده با تضمین کیفیت، قیمت رقابتی و پشتیبانی واقعی/پشتیبانی ۲۴ ساعته، تحویل سریع، رضایت مشتریان، و ضمانت چهار اصل کلیدی در مسیر فعالیت حرفه‌ای ما هستند.| [![apple id city logo](https://github.com/omariscoming/docs-appleidapi/blob/ddf6636ccd522c0426a051565625ef546b89c935/Logo-512px.png)](https://appleidcity.com) |


**شهر اپل آیدی** سرویس تخصصی در حوزه فروش و ارائه اپل‌آیدی است که امکان دسترسی مستقیم، سریع و امن به دیتابیس شهر اپل‌آیدی را فراهم می‌کند.

با استفاده از این API، توسعه‌دهندگان می‌توانند بدون نیاز به خرید یا ذخیره‌سازی قبلی اپل‌آیدی‌ها، به‌صورت آنی و از طریق درخواست‌های برنامه‌نویسی، اپل‌آیدی دریافت کرده و آن را در ربات‌ها، وب‌سایت‌ها و سایر پلتفرم‌های خود به‌کار بگیرند.
این سرویس با هدف ساده‌سازی فرایند تهیه اپل‌آیدی و ارائه یک راهکار قابل اطمینان برای فروشندگان و توسعه‌دهندگان طراحی شده و تجربه‌ای سریع، پایدار و یکپارچه را در اختیار کاربران قرار می‌دهد.


## ویژگی‌ها ✨

- ⚡ **دریافت اپل‌آیدی به‌صورت آنی**: بدون نیاز به ذخیره یا خرید قبلی  
- 🔒 **امنیت بالا**: استفاده از توکن اختصاصی برای هر کاربر  
- 🤖 **قابل استفاده در ربات‌ها و وب‌سایت‌ها**: راحت و سریع  
- 🛠️ **پیاده‌سازی آسان**: فقط با چند درخواست API ساده  
- 📈 **پشتیبانی از مقیاس‌پذیری**: مناسب پروژه‌های کوچک و بزرگ  


## دریافت کلید API

برای دریافت کلید امنیتی (API Key) مراحل زیر را دنبال کنید:

1. ابتدا **پنل همکاری** خود را در [ربات شهر اپل‌آیدی](https://t.me/appleidcitybot) فعال کنید.
2. از داخل ربات، دستور زیر را برای ایجاد **توکن امنیتی** ارسال کنید:
```
/generateapitoken
```
پس از انجام این مراحل، کلید امنیتی شما فعال خواهد شد و می‌توانید آن را در برنامه‌ها، وب‌سایت‌ها یا ربات‌های خود استفاده کنید.


برای استفاده از API، ابتدا باید **توکن امنیتی** خود را از [ربات شهر اپل‌آیدی](https://t.me/appleidcitybot) دریافت کرده باشید.
### ارسال توکن در Header
```
Authorization: YOUR_API_TOKEN
Content-Type: application/json
```
### قوانین دسترسی
- اگر هدر **Authorization** موجود نباشد → پاسخ `401 Unauthorized`  
- اگر **توکن نامعتبر** باشد → پاسخ `401 Unauthorized`  
- کاربر باید نوع `همکاری` داشته باشد → در غیر این صورت پاسخ `403 Forbidden`  
- اگر کاربر **مسدود شده** باشد → پاسخ `403 Forbidden` 

### نمونه پاسخ خطا
```json
{
  "success": false,
  "error": { "message": "Authorization header missing" }
}
```
```json
{
  "success": false,
  "error": { "message": "Invalid API token" }
}
```
```json
{
  "success": false,
  "error": { "message": "Access denied: insufficient permission level." }
}
```
```json
{
  "success": false,
  "error": { "message": "Internal server error" }
}
```

---
## 🧭 نحوه استفاده (Usage)

تمام مسیرها با پیش‌وند کلی زیر هستند:
```
https://api.appleidcity.com/appleid
```

> همه مسیرها نیاز به **توکن امنیتی** دارند.

### مسیرها (Endpoints)

| مسیر              | توضیح                             | متد |
|------------------|---------------------------------|------|
| `/buy`           | خرید اپل‌آیدی                     | POST |
| `/products`      | دریافت لیست محصولات               | GET  |
| `/user`          | دریافت اطلاعات مختصر کاربر        | GET  |

---
### 4.1 خرید اپل‌آیدی `/buy`

**Body (JSON):**
| پارامتر  | نوع     | توضیح                                         |
|----------|---------|-----------------------------------------------|
| icloud   | boolean | مشخص کردن اینکه اپل‌آیدی آیکلود فعال باشد یا خیر |

**نمونه درخواست با curl:**
```bash
curl -X POST "https://api.appleidcity.com/appleid/buy" \
-H "Authorization: YOUR_API_TOKEN" \
-H "Content-Type: application/json" \
-d '{"icloud": true}'
```

**نمونه پاسخ موفق:**
```json
{
  "success": true,
  "message": "Apple ID delivered successfully",
  "data": {
    "credentials": {
      "mail": "example@apple.com",
      "password": "Abc123456",
      "birthdate": "01/01/1990",
  "questions": {
    "first": {"question": "question1", "answer": "answer1"},
    "second": {"question": "question2", "answer": "answer2"},
    "third": {"question": "question3", "answer": "answer3"}
  },
    "product": {
      "name": "Apple ID WITH iCLOUD",
      "price": 1000000,
      "finalPrice": 950000,
      "category": "appleid",
      "icloud": true
    },
    "user": {
      "ready": 1,
      "discountPercentage": 5,
      "balance": 1240000
    }
  }
}
```

---
### 4.2 دریافت لیست محصولات `/products`

**نمونه درخواست با curl:**
```bash
curl -X GET "https://api.appleidcity.com/appleid/products" \
-H "Authorization: YOUR_API_TOKEN"
```

**نمونه پاسخ موفق:**
```json
{
  "success": true,
  "message": "Product List delivered successfully",
  "data": {
    "products": [
      {
        "name": "Apple ID without iCloud",
        "price": 10,
        "finalPrice": 9,
        "category": "appleid",
        "icloud": true
      },
      {
        "name": "Apple ID with iCloud",
        "price": 12,
        "finalPrice": 10.8,
        "category": "appleid",
        "icloud": false
      }
    ]
  }
}
```

---
### 4.3 دریافت اطلاعات کاربر `/user`

**نمونه درخواست با curl:**
```bash
curl -X GET "https://api.appleidcity.com/appleid/user" \
-H "Authorization: YOUR_API_TOKEN"
```

**نمونه پاسخ موفق:**
```json
{
  "success": true,
  "message": "User information delivered successfully",
  "data": {
    "balance": 100,
    "totalBalance": 500,
    "ready": 3,
    "custom": 1
  }
}
```
## 5️⃣ نکات مهم ⚠️

- مسیرهای `/buy`, `/products` و `/user` **همه نیاز به توکن امنیتی دارند**.  
- توکن را از [ربات شهر اپل‌آیدی](https://t.me/appleidcitybot) دریافت کنید.  
- مقادیر `finalPrice` بر اساس تخفیف کاربر محاسبه می‌شوند.  
- پاسخ‌های خطا شامل موجودی ناکافی، توکن نامعتبر و محصول موجود نیست می‌باشند.  
- تمام مسیرها با پیش‌وند `/appleid` ساخته شده‌اند.  

---

## 6️⃣ پشتیبانی

برای دریافت پشتیبانی و سوالات، می‌توانید از طریق [ربات شهر اپل‌آیدی](https://t.me/appleidcitybot) با ما در
---

